#include "philo.h"

/*void	init_threads(t_data *a)
{
	pthread_t	philo[a->nb_philo];
	int			i;

	i =  0;
	while (++i < a->nb_philo)
		pthread_create(&philo[i], NULL, &routine, NULL);
	i = 0;
	while (++i < a->nb_philo)
		pthread_join(&philo[i], NULL);
}

void	start_sim(t_data *a)
{
	init_thread();
}*/
